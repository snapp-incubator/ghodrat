apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ghodrat-janus.fullname" . }}-test-connection"
  labels:
    {{- include "ghodrat-janus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ghodrat-janus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
