apiVersion: v1
kind: Service
metadata:
  name: {{ include "ghodrat-janus.fullname" . }}
  labels:
    {{- include "ghodrat-janus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ghodrat-janus.selectorLabels" . | nindent 4 }}
